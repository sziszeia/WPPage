<div class="container-flex">
<p id=”notice” class=""><span style="color: green"><%= notice %></p></span>
<p>This is the shop area. Please check back regularly as we have a quick stock turn around</p>
<p>Our stock and products regulary update so do not miss out on good deals! Also help us raise money for good cause</p>
    <%if @cart_items.size > 0%>
        <div class="card justify-content-cener m-2" style="background-color: #d9ffb3; width: 50%; height: auto;">
            <p>You have <%=@cart_items.size%> items in your cart for £<%=@total%>. <span class="float-right"><a class="btn btn-info m-2" href="/shop/cart">Go to cart</a></span></p>
        </div>
    <% end %>
    <%if @products && @products.size != 0%>
        <div class="card justify-content-cener" style="width: 50%; height: 50em; overflow:auto;">
            <% @products.each do |product|%>
                <div class="card m-1" style="width: 100%; background-color:#e9e9e9">
                    <div class="card-body">
                        <%if product.isNew%>
                            <div class="card float-right" style="background-color: yellow; width: 4em">NEW</div>
                        <%end%>
                        <%if product.quantity == 0 %>
                            <div class="card float-right" style="background-color: red; width: 6em">Out of stock </div>
                        <% else %>
                            <div class="float-right">
                                <%= link_to 'Add to cart', add_to_cart_path(product), :class => "btn btn-small btn-info", method: :post %>
                            </div>
                        <% end %>
                        <p><strong><%=product.name%></strong>
                            <div class="float-right"><strong>£<%=product.price%></strong></div></p>
                        <h6>Description</h6><hr />
                        <p><%=product.description%></p>
                        <p><%=product.quantity%> remaining</p>
                    </div>
                </div>
            <%end%>
        </div>
    <%else%>
        <h2>Sorry, no products are available at the moment. Please check back later.</h2>
    <%end%>
</div>